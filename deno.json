{
  "name": "@levibostian/sh-style",
  "exports": "./main.ts",
  "publish": {
    "include": [
      "README.md",
      "LICENSE",
      "src/**/*.ts",
      "*.ts"
    ]
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1.0.18"
  },
  "tasks": {
    "run": "deno run --allow-env main.ts",
    "compile": "deno compile --output $OUTPUT_FILE_NAME --allow-env --target $DENO_TARGET main.ts",
    "test": "deno test --allow-env --allow-read --allow-run --allow-write --junit-path reports/junit.xml --coverage=reports/coverage/ --coverage-raw-data-only && deno coverage reports/coverage/ --include=$(pwd) && deno coverage reports/coverage/ --include=$(pwd) --lcov --output=reports/coverage.lcov"
  },
  "fmt": {
    "useTabs": false,
    "lineWidth": 100,
    "indentWidth": 2,
    "semiColons": true
  },
  "lint": {
    "rules": {
      "tags": ["recommended"]
    }
  }
}
